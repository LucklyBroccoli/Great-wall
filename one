int main()
{
	long long int x, y;
	scanf("%lld", &x);
	x >= -1e15 && x <= 1e15;
	if (x < 1||x<0)
	{
		y = x;
		printf("%lld", y);
	}
		
	else if (x >= 1 && x < 10)
	{
		y = 2 * x - 1;
		printf("%lld", y);
	}
	else
	{
		y = 3 * x - 11;
		printf("%lld", y);
	}
	
	return 0;
}
